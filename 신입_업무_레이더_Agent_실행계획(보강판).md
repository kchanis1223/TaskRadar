# 신입 업무 레이더 Agent — 실행계획 (보강판)

> 이 문서는 기존 `신입_업무_레이더_Agent_기획안.md`의 **컨셉을 유지한 채, 실제 제작·제출이 가능하도록 구체화한 실행 계획**입니다.
> 기존 기획안이 "무엇을 왜 만드는가"라면, 이 문서는 **"무엇을, 어떤 순서로, 어떤 도구로, 어떻게 검증해 제출하는가"**를 다룹니다.
> 기준일: 2026-06-24 (수)

---

## 0. 가장 먼저 알아야 할 것 — 제출양식이 요구하는 것

제출양식(`AI에이전트 사전과제_제출양식.docx`)을 분석하면, 이 과제는 **기획서만으로 끝나지 않습니다.** 다음을 반드시 채워야 합니다.

| 양식 항목 | 요구 내용 | 현재 기획안 상태 | 보강 필요 |
|---|---|---|---|
| 에이전트 이름 | 확정된 이름 | 있음("신입 업무 레이더 Agent") | 그대로 사용 가능 |
| **에이전트 URL** | **본인 외 타인도 접속 가능한 URL** | 없음 | **★실제 배포 필요 (가장 큰 갭)** |
| 사용 Tool | 예) Python, LangChain, n8n, Dify, GPTs | "제안" 수준 | 한 가지로 **확정 필요** |
| 01 에이전트 소개 | 기능 간략 설명 | 풍부함 | 1~2문장으로 압축 |
| 02 도입 효과 | AS-IS / TO-BE, **수치 효과** | 정성적 서술만 있음 | **정량 지표 필요** |
| 03 스크린샷 | 시작 화면 + 실행 결과 화면 | 없음 | **동작하는 화면 캡처 필요** |

**핵심 결론:** 이 과제의 합격선은 "기획의 완성도"가 아니라 **"접속 가능한 URL + 동작 스크린샷 + 수치화된 효과"**를 갖췄는지입니다.
따라서 실행계획의 1순위는 **"가장 적은 노력으로 공개 URL과 동작 화면을 확보하는 경로"**를 정하는 것입니다.

---

## 1. 도구 선택 — 비교와 추천

도구가 미정이므로, "공개 URL 확보 난이도"를 기준으로 현실적인 선택지를 비교합니다.

| 도구 | 공개 URL 확보 | 비용 | 카톡 텍스트 입력 | 코딩 | 데모/스크린샷 적합성 | 비고 |
|---|---|---|---|---|---|---|
| **Python + Streamlit** | `*.streamlit.app` 무료 배포 (GitHub 계정만 필요) | 무료 | 자유로움(텍스트 박스) | 필요(중) | 매우 좋음 | 화면·기능 자유도 최고 |
| **GPTs (ChatGPT)** | "링크가 있는 누구나" 공유 | **제작에 ChatGPT Plus($20/월) 필요, 열람자도 ChatGPT 로그인 필요** | 가능하나 긴 대화 붙여넣기 제약 | 불필요 | 보통 | 빠르지만 접근성·구독 제약 |
| **Dify (Cloud)** | 무료(Sandbox)에서 공개 웹앱 URL, 열람자 로그인 불필요 | 무료(메시지 크레딧·일일 한도 제약) | 가능 | 거의 불필요(노코드) | 좋음 | 한도 초과 시 데모 중단 위험 |
| LangChain/LangGraph 단독 | 별도 호스팅 필요(직접 배포) | 호스팅에 따라 | 자유로움 | 필요(상) | UI를 따로 만들어야 | URL 확보가 오히려 번거로움 |

### 추천: **Python + Streamlit (Streamlit Community Cloud 배포)**

이유:

1. **URL 요구를 무료·안정적으로 충족** — GitHub 계정만 있으면 `*.streamlit.app` 공개 주소가 나오고, 열람자에게 로그인을 요구하지 않습니다. 메시지 크레딧 같은 사용량 한도도 없어 평가자가 여러 번 눌러봐도 끊기지 않습니다.
2. **기획안의 기능 구조와 1:1로 맞음** — 카톡 텍스트 붙여넣기 → 분석 → 표/체크리스트/.ics 출력이라는 흐름을 화면으로 그대로 구현할 수 있습니다.
3. **스크린샷이 잘 나옴** — 시작 화면(입력창)과 실행 결과 화면(요약·일정·확인질문 표)을 명확히 캡처할 수 있어 양식의 03번을 충족하기 좋습니다.
4. **확장 메시지와 일치** — 기획안의 "메신저 비종속 어댑터" 구조는 Python 모듈로 자연스럽게 표현됩니다.

> 대안: **코딩을 최소화하고 싶다면 Dify**가 차선입니다. 단, 무료 한도(메시지 크레딧)가 있어 제출 직전 데모 시 한도 소진에 유의해야 합니다. **GPTs**는 가장 빠르지만, 제작자 본인이 Plus 구독을 해야 하고 열람자도 ChatGPT 계정이 필요해 "타인 접속" 요건에서 마찰이 있습니다.

**→ 이하 실행계획은 Streamlit 기준으로 작성하되, 도구 무관하게 공통인 "분석 로직·프롬프트·출력 스키마"는 그대로 재사용 가능합니다.**

---

## 2. AS-IS / TO-BE — 수치화 (양식 02번 채우기)

양식은 "수치로 나타낼 수 있는 효과"를 요구합니다. 신입 1인 기준의 **추정·목표치**로 제시하고, 측정 방법까지 함께 둡니다. (제출 시 "추정"임을 밝히면 충분합니다.)

| 구분 | AS-IS (도입 전) | TO-BE (도입 후, 목표) |
|---|---|---|
| 대화에서 할 일 정리 시간 | 대화 1건 다시 읽고 일정·할 일 수기 정리 **건당 약 5~10분** | 붙여넣기 후 자동 정리 **건당 약 1분 이내 (약 80% 단축)** |
| 업무 요청 누락 | 메신저에 흩어진 요청을 **체감상 가끔 누락** | 추출 + 놓칠 위험도 표시로 **누락 위험 항목 0건 목표** |
| 애매한 요청 재확인 | 무엇을 물어야 할지 고민 → 확인 지연/생략 | **확인 질문 문장 자동 생성**으로 즉시 재확인 가능 |
| 제출 전 실수 | 양식·파일명·공유 대상 등 **사후 발견** | **제출 전 체크리스트**로 사전 점검 |

### 효과를 "측정"하는 방법 (스크린샷·근거 확보용)

- **시간 단축**: 동일 샘플 대화를 (1) 수기로 정리하는 시간, (2) Agent로 정리하는 시간을 각각 재서 비교 → "X분 → Y분, Z% 단축"으로 기재.
- **누락 방지**: 샘플 대화 5건에 일부러 일정·할 일·애매표현을 심어두고, Agent가 몇 개를 빠짐없이 잡아내는지 집계 → "심어둔 항목 12개 중 12개 추출(추출률 100%)" 형태.

> 이 두 수치는 **3장 실행 로드맵의 Phase 4(검증)** 에서 자연스럽게 산출됩니다.

---

## 3. 단계별 실행 로드맵

마감이 정해지지 않았으므로 날짜 대신 **단계(Phase)와 완료 기준(Definition of Done)**으로 구성합니다. 각 Phase는 독립적으로 "동작하는 결과물"을 남기도록 설계해, 중간에 멈춰도 제출 가능한 상태를 유지합니다.

### Phase 0 — 준비 (반나절)
- [ ] 도구 확정 (추천: Streamlit) 및 GitHub 계정/저장소 생성
- [ ] LLM 키 확보 (OpenAI API 또는 사내 제공 LLM) — **키는 코드에 하드코딩하지 않고 Streamlit Secrets/환경변수로 주입**
- [ ] 샘플 카톡 대화 3~5건 준비 (`sample_chat.txt`) — 명확/애매/상대날짜가 섞이도록
- **완료 기준:** 빈 Streamlit 앱이 로컬에서 실행됨

### Phase 1 — 분석 코어 (1~2일) ★가장 중요
- [ ] `messenger_parser.py`: 카톡 내보내기 텍스트 → 공통 `{sender, time, message}` JSON 변환
- [ ] `agent_prompt.py`: 분석 프롬프트 + 출력 JSON 스키마 (→ 4장 참조)
- [ ] `task_extractor.py`: LLM 호출 → JSON 파싱 → 일정/할일/확인필요 분류
- [ ] `risk_scorer.py`: 놓칠 위험도 점수화 (→ 5장 규칙)
- [ ] `reminder_generator.py`: 업무유형별 리마인드 시점 계산
- **완료 기준:** `sample_chat.txt`를 넣으면 콘솔에서 요약·일정·할일·확인질문·위험도 JSON이 출력됨 (UI 없이)

### Phase 2 — 화면 (1일)
- [ ] `app.py`: 입력 텍스트박스 + **기준일 선택(상대날짜 해석용)** + 실행 버튼
- [ ] 결과 표시: ① 요약 ② 확정 일정 ③ To-do 후보 ④ 확인 필요 ⑤ 놓칠 위험도 ⑥ 리마인드 ⑦ 선배 확인 문장 ⑧ 제출 체크리스트
- [ ] `calendar_exporter.py`: 확정/후보 일정 → `.ics` 다운로드 버튼
- **완료 기준:** 브라우저에서 붙여넣기 → 버튼 → 전 항목이 화면에 보임

### Phase 3 — 배포 (반나절)
- [ ] GitHub 푸시 → Streamlit Community Cloud 연결 → 배포
- [ ] 커스텀 서브도메인 지정 (예: `newbie-task-radar.streamlit.app`)
- [ ] **시크릿(API 키) 설정**, 다른 기기/계정에서 접속 테스트
- **완료 기준:** 외부에서 접속 가능한 URL 확보 (양식 "에이전트 URL" 충족)

### Phase 4 — 검증 & 제출물 (반나절)
- [ ] 6장 테스트셋으로 추출률·분류 정확도 점검
- [ ] 2장 AS-IS/TO-BE 수치 실제 측정
- [ ] **스크린샷 2종 캡처** (시작 화면 / 실행 결과 화면)
- [ ] 제출양식 docx 작성: 이름·URL·Tool·소개·AS-IS/TO-BE·스크린샷
- **완료 기준:** 제출양식 모든 칸이 채워지고 URL이 정상 동작

> **최소 동작 우선순위(시간이 빠듯하면):** Phase 1의 요약·일정/할일 추출·확인질문 생성까지가 "필수 코어"입니다. 위험도 점수화·리마인드·체크리스트·.ics는 그다음, GPT 연동 고도화는 마지막입니다.

---

## 4. 기술 설계 ① — 프롬프트와 출력 스키마

LLM 분석의 품질과 안정성은 **"구조화된 출력(JSON) 강제"**에서 나옵니다. 자유 서술 대신 아래 스키마로 받으면, 화면·`.ics`·체크리스트를 코드로 일관되게 처리할 수 있습니다.

### 4.1 출력 JSON 스키마

```json
{
  "summary": "대화 핵심 요약 (2~3문장)",
  "reference_date": "2026-06-24",
  "items": [
    {
      "id": "t1",
      "raw_text": "원문 메시지",
      "type": "일정 | 할일 | 확인필요",
      "title": "발표자료 제출",
      "datetime_start": "2026-06-26T18:00",   // 일정일 때 (없으면 null)
      "due": "2026-06-25T18:00",               // 할일/마감일 때 (없으면 null)
      "date_confidence": "높음 | 중간 | 낮음",
      "classification": "확정 | 후보 | 확인필요",
      "importance": "높음 | 중간 | 낮음",
      "miss_risk_score": 65,
      "miss_risk_level": "높음",
      "ambiguities": ["제출 방식 미정", "양식 추후 공유"],
      "suggested_question": "발표자료는 어디로 제출드리면 될까요?",
      "reminders": ["2026-06-25T09:00", "2026-06-25T15:00"],
      "checklist": ["파일명 규칙", "분량", "오탈자", "양식", "공유 대상/경로"]
    }
  ],
  "questions_to_senior": "취합된 확인 문장 (한 번에 정중히 보낼 수 있게)"
}
```

### 4.2 시스템 프롬프트 골격

```
[역할] 너는 신입 직원의 업무 보조 Agent다. 메신저 대화에서 일정·할 일·확인 필요사항을 빠짐없이 찾아낸다.
[기준일] 오늘은 {reference_date}. 모든 상대적 날짜 표현은 이 날짜를 기준으로 절대 날짜로 변환하라.
[추출 규칙]
- 일정: 날짜/시간이 있는 모임·발표·회의
- 할일: 사용자가 수행해야 하는 작업·제출·정리
- 확인필요: 범위/기한/우선순위가 애매한 항목
[분류 규칙]
- 확정: 날짜·시간이 명확
- 후보: 일정/마감으로 보이나 정보 일부 부족
- 확인필요: 날짜·범위·우선순위가 애매
[안전 규칙]
- 추측으로 일정을 단정하지 말 것. 불확실하면 confidence를 낮추고 확인필요로 둘 것.
- 절대 자동 등록을 전제하지 말고 '후보'로만 제시할 것.
[출력] 위 JSON 스키마만 출력. 설명 문장 금지.
[예시] (대화 → JSON 1~2쌍의 few-shot)
```

> 핵심: **기준일 주입**과 **JSON-only 출력**, **불확실=확인필요** 원칙. 이 셋이 기획안 4.5(3단계 분류)·4.6(애매표현 감지)·13장(한계 보완)을 코드로 실현하는 장치입니다.

---

## 5. 기술 설계 ② — 날짜 처리와 위험도 점수화

### 5.1 상대 날짜 → 절대 날짜 (기준일 필수)

기획안 13장의 한계("날짜 표현이 상대적이면 기준일이 필요")를 다음 규칙으로 해소합니다. 화면 상단에서 **기준일을 선택**하게 하고(기본값 오늘), 프롬프트에 주입합니다.

| 표현 | 해석 규칙 (기준일=2026-06-24 수 예시) |
|---|---|
| 내일 | 기준일 +1일 → 2026-06-25 |
| 이번 주 금요일 | 같은 주 금요일 → 2026-06-26 |
| 다음 주 초 | 다음 주 월요일로 가정하되 **confidence 낮음 + 확인필요** |
| 퇴근 전 | 시간 미상 → 18:00로 가정하되 **확인질문 생성** |
| 추후 공유 | 날짜 없음 → 일정 미생성, 확인필요로 분류 |

### 5.2 놓칠 위험도 점수화 규칙 (정량화)

기획안 4.8의 정성 기준을 **가산점 규칙**으로 바꿔, 코드로 재현 가능하게 만듭니다.

| 조건 | 가산점 |
|---|---:|
| 기한이 24시간 이내로 임박 | +30 |
| 날짜/시간이 불명확(상대표현·시간 미상) | +20 |
| 산출물 형식이 불명확 | +20 |
| 제출/공유 방식이 미정 | +15 |
| 후속 정보 대기("추후 공유" 등) | +15 |

**위험 등급:** 0–25 낮음 · 26–50 중간 · 51+ 높음
**중요도(importance)는 별도**로 — 요청 강도·기한 자체의 중대성으로 판단해 위험도와 분리 표기.

> 예) "AI 아이디어 내일까지 정리": 기한 임박(+30) + 산출물 형식 불명확(+20) = 50 → 중간~높음 경계. "제출 양식 추후 공유": 방식 미정(+15) + 후속 대기(+15) = 30 → 중간이지만 **누락 가능성 강조 대상**.

---

## 6. 검증 & 테스트 계획

제출의 신뢰도(와 02번 수치 근거)를 위해, 가벼운 자체 테스트셋을 둡니다.

### 6.1 테스트 대화 5종 (의도적으로 난이도 섞기)
1. **명확형** — 날짜·시간 분명한 회의/발표
2. **상대날짜형** — "내일", "다음 주 초", "이번 주 금요일"
3. **애매표현형** — "가능하면", "한번 봐주세요", "퇴근 전"
4. **혼합형** — 일정+할일+확인필요가 한 대화에 공존 (기존 기획안 10장 시나리오)
5. **함정형** — 잡담 속에 업무 요청 1건만 섞기 (오탐/누락 점검)

### 6.2 측정 지표
- **추출률** = 심어둔 업무 항목 중 잡아낸 비율 (목표 100%)
- **분류 정확도** = 확정/후보/확인필요가 의도대로 분류된 비율
- **오탐률** = 잡담을 업무로 잘못 잡은 건수 (낮을수록 좋음)
- **시간 비교** = 수기 정리 시간 vs Agent 정리 시간

### 6.3 데모/스크린샷 시나리오
- 시작 화면: 빈 입력창 + 기준일 선택 + "분석하기" 버튼
- 실행 결과 화면: 4번 혼합형 대화 입력 결과 (요약·일정·할일·확인질문·위험도가 한 화면에)

---

## 7. 리스크 & 운영 주의 (보강)

기획안 13장에 더해, **실제 배포 시** 챙길 항목입니다.

| 리스크 | 대응 |
|---|---|
| API 키 노출 | 코드에 키 금지, Streamlit Secrets/환경변수 사용, 저장소 공개 시 키 비포함 |
| 무료 한도/비용 | LLM 호출량 관리, (Dify 선택 시) 메시지 크레딧 소진 주의 |
| 붙여넣은 대화 속 악성 지시(프롬프트 인젝션) | 입력 대화는 "분석 대상 데이터"로만 취급하도록 프롬프트에 명시, 대화 내 지시는 실행하지 않음 |
| 개인정보(PII) | 데모용 가상 대화 사용, 실명·연락처 등 비포함 |
| 잘못된 자동 일정화 | 모든 일정은 "후보"로만 제시, 사용자 확인 후 .ics 다운로드 (자동 등록 없음) |

---

## 8. 제출양식 작성 가이드 (그대로 옮겨 쓰기용)

| 양식 칸 | 채울 내용(초안) |
|---|---|
| 에이전트 이름 | 신입 업무 레이더 Agent |
| 에이전트 URL | (Phase 3에서 확보한 `*.streamlit.app` 주소) — 타 기기 접속 확인 후 기재 |
| 사용 Tool | Python, Streamlit, OpenAI API (LLM) |
| 01 소개 | "메신저 대화를 붙여넣으면 일정·할 일·마감을 자동 추출하고, 애매한 요청은 확인 질문까지 만들어 주는 신입 업무 보조 Agent" |
| 02 AS-IS | 메신저에 흩어진 요청을 다시 읽고 수기로 정리(건당 5~10분), 일부 누락·확인 지연 발생 |
| 02 TO-BE | 붙여넣기→자동 정리 1분 이내(약 80% 단축), 놓칠 위험도 표시로 누락 위험 0건 목표, 확인 질문 자동 생성 |
| 03 스크린샷 | 시작 화면 / 혼합형 대화 실행 결과 화면 |

---

## 9. 다음 액션 (바로 시작 가능)

1. **도구 확정** — Streamlit 추천대로 갈지 결정 (미정 시 이 문서 1장 비교표로 판단)
2. **Phase 0 착수** — GitHub 저장소 + 샘플 대화 + LLM 키 준비
3. **Phase 1 코어 먼저** — UI보다 "분석 JSON이 제대로 나오는가"를 먼저 완성

> 원하면 이 실행계획을 바탕으로 **Phase 1 코어 코드(파서·프롬프트·추출기) 초안**을 바로 작성해 드릴 수 있습니다.
