# TaskRadar 실행 가이드 (내 PC, 폴백 모드)

신입 업무 레이더 Agent — 카카오톡 대화 `.txt`를 분석해 업무/일정/추천 문구/오전 8시 알림을 생성하는 Streamlit MVP.

## 사전 준비

- Python 3.9 이상 (검증은 3.10에서 완료)
- 프로젝트 루트: `...\TaskRadar\TaskRadar` (`app.py`가 있는 폴더)

## 실행 (Windows PowerShell)

```powershell
cd "$env:USERPROFILE\OneDrive\Desktop\TaskRadar\TaskRadar"

# 1) 가상환경 생성·활성화
python -m venv .venv
.\.venv\Scripts\Activate.ps1

# 2) 의존성 설치
pip install -r requirements.txt

# 3) 앱 실행 (브라우저가 자동으로 열림)
streamlit run app.py
```

실행되면 브라우저에서 `http://localhost:8501` 이 열립니다.

> PowerShell에서 `Activate.ps1` 실행이 막히면:
> `Set-ExecutionPolicy -Scope Process -RemoteSigned` 한 번 실행 후 다시 활성화.

## 사용 흐름

1. 사이드바에서 모드 선택: `local`(파일 업로드 중심) / `demo`(샘플로 바로 시연).
2. 기준일 선택 → 카카오톡 `.txt` 업로드하거나 대화 붙여넣기 → **분석하기**.
3. 탭에서 결과 확인: 요약 / 업무 요청 / 추천 문구 / To-do·일정 / 알림 미리보기 / JSON.
4. 선배 답변을 받으면 하단에 붙여넣고 **답변 반영하기** → To-do 세부정보(양식·분량·제출 경로) 자동 보강.

빠르게 보고 싶으면 **샘플 대화 사용** 토글을 켜면 됩니다 (`sample_chat.txt`).

## LLM provider (실제 AI 연동)

프로젝트 루트(`app.py`가 있는 폴더)에 `.env` 파일을 만들면 앱이 자동으로 읽습니다.
API 키가 없으면 **규칙 기반 폴백**으로 동작하고, API 호출 실패 시 화면에 provider 경고가 표시됩니다.
데모 서비스에서는 사용자가 API 키를 입력하지 않습니다. 운영자 키는 서버 `.env` 또는 배포 환경의 secret에만 저장합니다.

| 환경변수 | 효과 |
|---|---|
| (없음) | 규칙 기반 폴백 — 기본값 |
| `TASKRADAR_USE_OPENCODE=1` | 로컬 opencode OAuth provider 사용 |
| `TASKRADAR_OPENCODE_COMMAND` | opencode 실행 파일명 또는 전체 경로, 기본 `opencode` |
| `TASKRADAR_OPENCODE_MODEL` | opencode에서 사용할 모델명, 기본 `openai/gpt-5.4-mini` |
| `TASKRADAR_OPENCODE_TIMEOUT` | opencode 실행 제한 시간(초), 기본 180 |
| `OPENAI_API_KEY` | OpenAI provider 사용 (모델 기본 `gpt-4o-mini`, `OPENAI_MODEL`로 변경) |
| `OPENAI_MODEL` | 사용할 OpenAI 모델명 |
| `OPENAI_TIMEOUT` | OpenAI 요청 제한 시간(초), 기본 45 |
| `ANTHROPIC_API_KEY` | Anthropic provider 사용 (OpenAI 키가 없거나 OpenAI 호출 실패 시 시도) |
| `ANTHROPIC_MODEL` | 사용할 Anthropic 모델명 |
| `ANTHROPIC_TIMEOUT` | Anthropic 요청 제한 시간(초), 기본 45 |
| `GEMMA_API_URL` | 동일 JSON 스키마를 반환하는 Gemma 서버 사용 (OpenAI 다음으로 시도) |
| `TASKRADAR_MODE=demo` | 시작 시 demo 모드 |

OpenAI로 실제 분석을 붙이려면 `.env` 파일에 아래처럼 입력하세요.

```env
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
```

Anthropic 키를 운영자 키로 사용할 수도 있습니다.

```env
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-3-5-haiku-latest
```

그 다음 평소처럼 실행하면 됩니다.

로컬 구독/OAuth 인증을 opencode에 맡기려면 먼저 opencode에서 Anthropic 로그인을 완료한 뒤 `.env`에 스위치를 켭니다.

```powershell
opencode providers login anthropic
```

```env
TASKRADAR_USE_OPENCODE=1
TASKRADAR_OPENCODE_COMMAND=opencode
TASKRADAR_OPENCODE_MODEL=openai/gpt-5.4-mini
```

이 방식에서는 TaskRadar가 opencode OAuth 토큰 파일을 직접 읽지 않습니다. 대화 분석 요청은 `opencode run`에 첨부 파일로 전달되고, opencode가 인증과 모델 호출을 처리합니다.
현재 터미널에서 `opencode --version`이 동작하지 않으면 `TASKRADAR_OPENCODE_COMMAND`에 `opencode.cmd`의 전체 경로를 넣거나 opencode 설치 경로를 PATH에 추가해야 합니다.

보안 원칙:

- `.env`는 git에 올리지 않습니다.
- 사용자 화면에는 API key, provider 원문 오류, 내부 로그를 표시하지 않습니다.
- 개발 중 provider 상태를 자세히 확인해야 할 때만 `TASKRADAR_DEBUG=1`을 켭니다.
- 공개 데모에서는 `TASKRADAR_DEBUG`를 비워둡니다.

## 테스트

```powershell
pytest
```

(messenger_parser 2건 + task_extractor 2건, 총 4건)

## 폴더 구조

```
app.py                  Streamlit UI (탭 6개, 분석/답변 반영 버튼)
task_extractor.py       핵심: provider 선택(Gemma→OpenAI→폴백) + 폴백 규칙 분석
agent_prompt.py         LLM용 프롬프트 + JSON 스키마
messenger_parser.py     카카오톡 내보내기 .txt 파서 (괄호/쉼표 포맷, 멀티라인)
date_utils.py           상대 날짜·시간 추론 (내일/오늘/요일/N시)
risk_scorer.py          누락 위험도 점수화 (기한 임박/모호 표현/형식 미정 등)
reminder_generator.py   리마인더 시각 + 오전 8시 알림 문구 생성
notification_provider.py 알림 미리보기 provider (카카오 발송은 2차 확장 placeholder)
models.py               데이터클래스 (ChatMessage / AnalysisItem / AnalysisResult)
sample_chat.txt         데모용 샘플 대화
tests/                  pytest
```
